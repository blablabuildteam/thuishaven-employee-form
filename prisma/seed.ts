/**
 * HR users are configured via HR_USERS in .env.local — no DB seed needed.
 * Keep this file as a placeholder for future demo data.
 */
console.log(
  "No DB seed required. Configure HR accounts with HR_USERS in .env.local.",
);
